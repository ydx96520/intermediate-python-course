import os

print('Process(%s) is start' % os.getpid())

pid=os.fork()
if pid==0:
	print('I am chid process(%s) and parent is %s' % (os.getpid(), os.getppid()))
else:
	print('I am parent process (%s) and chid is %s' % (os.getpid(), pid))
